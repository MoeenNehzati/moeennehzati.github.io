---
---
<section>
  <h1><a href="{{ entry.url }}">{{ entry.title }}</a></h1>
  <p>
    {{ entry.note }}{% if entry.credit_url %} <a href="{{ entry.credit_url }}">{{ entry.credit_text }}</a>.{% endif %}
  </p>
</section>
